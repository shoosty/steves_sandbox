Highlights:
db/Faker




set up github
  go to github and crate reposititory
  use the https method

set up database
console
rails generate (gives help messages)
  rails generate migration steves_sandbox  (creates a migration)
run migration
  rake db:migrate
  note:
    tables are plural
    classes are singular except controllers which are plural!
example:
rails generate migration CreateMountain mountain:text height:integer notes:text


rails generate migration AddFieldsToSandbox user:text email:text fname:text lname:text password:text
rails generate controller mountain   (not correct)
rails generate controller mountains (correct - notice plural)
rake db:migrate

rails generate migration AddFieldsToMountain planet:text
rails generate migration AddEmailToMountain record_added_by_email:text

.....
clean code with atom - open command pallette and choose autoindent
command - shift - p
seek auto indent
.....

Active Record environment
require File.expand_path('../../config/environment', __FILE__)

Active Record tests

Sandbox.where("id = 311")
Sandbox.where("id >= 211 and id <=400")
Sandbox.where "user LIKE '%Paul%'"

....
make nicely formated records
gem install table_print


action name must match the view name

... sql to test records ...
select * from sandboxes order by id desc limit 10;


>>>>>>>>>>
to start a new MVC
1. rails generate model user first_name last_name email age:integer
2. rake db:migrate

>>>>>>>


Adding Twitter Bootstrap
gem "twitter-bootstrap-rails"
bundle install
rails generate bootstrap:install static
rails generate simple_form:install --bootstrap
rails g bootstrap:layout [LAYOUT_NAME]
